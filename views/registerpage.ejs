<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Page</title>
    <%- include('partials/style') %>
  </head>
  <body>
    <%- include('partials/navbar') %>

    <div class="container header">
      <h1>Register Account in Halodek</h1>
    </div>
    <div class="container login-form">
      <form action="/user/register" method="post">
        <label for="username">Username</label>
        <input type="text" name="username" />
        <% if (errQuery?.username) { %>
        <span style="color: red"><%= errQuery?.username %> </span><br /><br />
        <% } %>

        <label for="email">Email</label>
        <input type="text" name="email" />
        <% if (errQuery?.email) { %>
        <span style="color: red"><%= errQuery?.email %> </span><br /><br />
        <% } %>

        <label for="password">Password</label>
        <input type="password" name="password" />
        <% if (errQuery?.password) { %>
        <span style="color: red"><%= errQuery?.password %> </span><br /><br />
        <% } %>

        <label for="fullName">Full Name</label>
        <input type="text" name="fullName" />

        <label for="age">Age</label>
        <input type="number" name="age" />

        <label for="">Gender</label><br />

        <input type="radio" name="gender" value="Male" />
        <label for="gender">Male</label>

        <input type="radio" name="gender" value="Female" />
        <label for="gender">Female</label>

        <input type="submit" value="Register" class="register" />
      </form>
      <a href="/user/login"
        ><input type="submit" value="Cancel" class="cancel"
      /></a>
    </div>
  </body>
</html>
